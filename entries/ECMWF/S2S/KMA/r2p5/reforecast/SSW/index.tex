\begin{ingrid}
continuedataset:


grid:
/name (S) def
/units (days since 1960-01-01) def
/long_name (forecast start time) def
/standard_name (forecast_reference_time) def
01 Nov 2016 julian_day 01 Jan 1960 julian_day sub
1.0
systemtime
(%d %b %Y) 12 strftime interp julian_day 63. sub
01 Jan 1960 julian_day sub
/pointwidth 0. def
/defaultvalue { last } def
:grid

  S monthlyedgesgrid
   name units gridtype mark gridvalues {dup 8 add dup 8 add dup 8 add}forall
                     pop
                     counttomark realarray astore
        nip NewGRID
    nip /pointwidth 0. def
  first secondtolast subgrid
/defaultvalue { last } def
   name exch def


S {(/Data/data25/ECMWF/S2S/KMA/REF/SSW/%Y[S]/%m[S]/SSW.kma.2010%m%d[S].%Y%m%d[S]) [S]} removeextrausing
name exch def

  S last cvsunits
    interp exch 0.5 add
       3 -1 roll
        ymd:nextmonth ymd:nextmonth
       ymd2systemtime
     nip
     /expires exch def
name exch def
/expires S  .expires def

grid:
/name /hdate def
/units (months since 1960-01-01) def
01 Jul 1991 ensotime
12.0
01 Jul 2010 ensotime
/defaultvalue { last } def
:grid

grid:
/name /L def
/long_name (Lead) def
/standard_name (forecast_period) def
/units (days) def
1. 1. 60.
/pointwidth 0. def
:grid

grid:
/name /M def
/long_name (Ensemble Member) def
/standard_name (realization) def
/units (ids) def
0 1 2
:grid


 /ensembles { [ M L | S hdate ] 
 (/Data/data25/ECMWF/S2S/KMA/REF/SSW/%Y[S]/%m[S]/SSW.kma.%Y[hdate]%m%d[S].%Y%m%d[S]) [ S hdate ]
readdatafile:
((59(3(25X,F6.7,/)),(2(25X,F6.7,/),(25X,F6.7)))) formatted
:readdatafile
/units (unitless) def
} defasvarsilent



:dataset
\end{ingrid}
