\begin{ingrid}
continuedataset:

link:
 /name (Model Versions) def
 /description (ECMWF page describing model versions) def
 /href (https://confluence.ecmwf.int/display/S2S/ECMWF+Model) def
:link

/note (See the "Model Versions" link in the "Documents" section of this page for information on past and current versions of the CPTEC S2S model) def

datasetdefs:

grid:
   /name /Smod def
   0 3.5 52
% the end year here must be manually advanced at the start of each year to the following year (i.e. use 2020 once 2019 has arrived as the start year in the S grid)
        7 mul
         systemtime
        (%d %b %Y) 12 strftime
        interp nip nip 1 add
        2015 sub mul


:grid
:datasetdefs

grid:
/name (X) def
/long_name (longitude) def
/units (degree_east) def
periodic
0.0 1.5 358.5
:grid

grid:
/name (Y) def
/long_name (latitude) def
/units (degree_north) def
-90.0 1.5 90.0
:grid

grid:
/name (S) def
/units (days since 1960-01-01) def
/long_name (forecast start time) def
/standard_name (forecast_reference_time) def
13 Dec 2023 julian_day 01 Jan 1960 julian_day sub
1.0
%7.0
systemtime
(%d %b %Y) 12 strftime interp julian_day
%27 Dec 2023 julian_day
01 Jan 1960 julian_day sub
/pointwidth 0. def
/defaultvalue { last } def
:grid

 /S Smod
    %0.5 add
    7 mod
    0 maskgt
    Smod add
    Smod
     2.5 sub
     replaceNaN
    /name /S def
    Smod
     0 add
     /name /Smod2 def
     exch use_as_grid
    .S
    /units (days since 2015-01-01) def
    /pointwidth 0 def
     dup
     (days since 1960-01-01) streamgridunitconvert
    /long_name (forecast start time) def
    /standard_name (forecast_reference_time) def
    /defaultvalue {last}def
    def

     S dup 13
     Dec 2023 julian_day
     01
      Jan 1960 julian_day
      sub
      %26 Jun 2023 julian_day
      systemtime
      (%d %b %Y) 12 strftime interp julian_day
      01 Jan 1960 julian_day sub
     subgrid
     second last subgrid
    replaceGRID

 S name units gridtype mark gridvalues
          {1 sub}forall counttomark realarray astore
          nip NewGRID
      /pointwidth 0.0 def
      /defaultvalue { last } def
      replaceGRID

S {(/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_da_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ]} removeextrausing

/expires
  S last
    1 add
    cvsunits nip interp exch
      0.5 add
      3 -1 roll
      ymd2systemtime
    nip
def
name exch def
/expires S  .expires def


% a grid for instantaneous values, starting at 1
grid:
/name /L def
/long_name (Lead) def
/standard_name (forecast_period) def
/units (days) def
0. 1. 34.
/pointwidth 0. def
:grid

% % a grid for instantaneous values, starting at 1
% grid:
% /name /L1 def
% /long_name (Lead) def
% /standard_name (forecast_period) def
% /units (days) def
% 1. 1. 35.
% /pointwidth 0. def
% :grid

% a lead grid for daily averages
grid:
/name /LA def
/long_name (Lead) def
/standard_name (forecast_period) def
/units (days) def
0.5 1. 34.5
/pointwidth 1.0 def
:grid

% % a lead grid for instantaneous values, starting from 6h
% grid:
% /name /L6 def
% /long_name (Lead) def
% /standard_name (forecast_period) def
% /units /days def
% 0. 0.25 35.
% /pointwidth 0. def
% :grid

% % a lead grid for 6-hrly averages or accumulation
% grid:
% /name /LA6 def
% /long_name (Lead) def
% /standard_name (forecast_period) def
% /units /days def
% 0.125 0.25 34.875
% /pointwidth 0.25 def
% :grid

% grid:
% /name (P) def
% /long_name (Pressure Level) def
% /standard_name (air_pressure) def
% /units (hPa) def
% values: 10 50 100 200 300 500 700 850 925 1000 :values
% :grid

dataset:
[X Y L S ] {name exch def } forall
/name /sfc_pressure def

/msl { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (PRES) def
  (04XXXX00010300XXXXXXXXXXXXXXXXXXXXXX65) GRIBvariable
  :readdatafile
  /long_name (Mean Sea Level Pressure) def
  /standard_name (air_pressure_at_sea_level) def
  /units (Pa) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

/sp { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (PRES) def
  (04XXXX00010300XXXXXXXXXXXXXXXXXXXXXX01) GRIBvariable
  :readdatafile
  /long_name (Surface Pressure) def
  /standard_name (air_pressure) def
  /units (Pa) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

dataset:
[X Y L S ] {name exch def } forall
/name /sfc_heat_flux def

/sshf { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (sshf) def
  % SHTFL
  (04XXXX000B000B) GRIBvariable
  :readdatafile
  /long_name (Surface sensible heat flux) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min -1.0E+09 def
  /scale_max 2.8E+08 def
  decompress
  Y last first RANGE
} defasvarsilent

/slhf { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (slhf) def
  % LHTFL
  (04XXXX000B000A) GRIBvariable
  :readdatafile
  /long_name (Surface latent heat flux) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min -1.5E+09 def
  /scale_max 3.0E+07 def
  decompress
  Y last first RANGE
  } defasvarsilent

:dataset

dataset:
/name (10m_above_ground) def
[X Y L S ] {name exch def } forall

  /10u { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (10u) def
  % UGRD
  (04XXXX00010202) GRIBvariable
  :readdatafile
  /long_name (10 metre u-velocity) def
  /standard_name (eastward_wind) def
  /units (m/s) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

/10v { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (10v) def
  % VGRD
  (04XXXX00010203) GRIBvariable
  :readdatafile
  /long_name (10 metre v-velocity) def
  /standard_name (northward_wind) def
  /units (m/s) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

dataset:
[X Y LA S ] {name exch def } forall
/name /sfc_rad_flux def

/ssrd { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (ssrd) def
  % DSWRF
  (04XXXX000B0407) GRIBvariable
  :readdatafile
  /long_name (Surface solar radiation downwards) def
  /standard_name (surface_downwelling_shortwave_flux_in_air) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 2.0E+09 def
  decompress
  Y last first RANGE
} defasvarsilent

/strd { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (strd) def
  % DLWRF
  (04XXXX000B0503) GRIBvariable
  :readdatafile
  /long_name (Surface thermal radiation downwards) def
  /standard_name (surface_downwelling_longwave_flux_in_air) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 2.0E+09 def
  decompress
  Y last first RANGE
} defasvarsilent

/ssr { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (ssr) def
  % NSWRF
  (04XXXX000B0409) GRIBvariable
  :readdatafile
  /long_name (Surface net solar radiation) def
  /standard_name (surface_net_downward_shortwave_flux) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 1.50E+09 def
  decompress
  Y last first RANGE
} defasvarsilent

/str { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (str) def
  % NLWRF surface
  (04XXXX000B0505XXXXXXXXXXXXXXXXXXXXXX01) GRIBvariable
  :readdatafile
  /long_name (Surface net thermal radiation) def
  /standard_name (surface_net_upward_longwave_flux) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min -6.5E+08 def
  /scale_max 7.0E+07 def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

dataset:
/name (top_of_atmosphere) def
[X Y LA S ] {name exch def } forall

/ttr { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (ttr) def
  % NLWRF toa
  (04XXXX000B0505XXXXXXXXXXXXXXXXXXXXXX08) GRIBvariable
  :readdatafile
  /long_name (Top net thermal radiation) def
  /standard_name (toa_net_upward_longwave_flux) def
  /units (W m-2 s) def
  /missing_value 9.999000E+20 def
  /scale_min -1.5E+09 def
  /scale_max 10000. def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

dataset:
[X Y L S ] {name exch def } forall
/name /sfc_precip def

/cp { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (cp) def
  % CPRAT
  (04XXXX000B0125) GRIBvariable
  :readdatafile
  /long_name (Convective precipitation) def
  /standard_name (convective_precipitation_flux) def
  /units (kg m-2) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 950. def
  decompress
  Y last first RANGE
} defasvarsilent

/sf { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (sf) def
  % TSRWE
  (04XXXX000B0135) GRIBvariable
  :readdatafile
  /long_name (Snow Fall water equivalent) def
  /standard_name (snowfall_flux) def
  /units (kg m-2) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 750. def
  decompress
  Y last first RANGE
} defasvarsilent

/tp { [ X Y last first subgrid | L S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (TPRATE) def
  (04XXXX000B0134) GRIBvariable
  :readdatafile
  /long_name (Total Precipitation) def
  /standard_name (precipitation_amount) def 
  /units (kg m-2) def
  /missing_value 9.999000E+20 def
  /scale_min 0. def
  /scale_max 1200. def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

dataset:
/name (2m_above_ground) def
[X Y LA S ] {name exch def } forall

% /2t { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/ECMF/CY48/REL/CF/%Y[S]/%m[S]/ecmf_rel_cf_da_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
%   /name (TMP) def
%   (04XXXX000B0000) GRIBvariable
%   :readdatafile
%   /long_name (2-meter Temperature) def
%   /standard_name (air_temperature) def
%   /units (K) def
%   /missing_value 9.999000E+20 def
%   decompress
%   Y last first RANGE
% } defasvarsilent

/mx2t6 { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (mx2t6) def
  (04XXXX000B0000XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX02020) GRIBvariable
  :readdatafile
  /long_name (2-meter Maximum Temperature) def
  /standard_name (air_temperature) def
  /units (K) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

/mn2t6 { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
  /name (mn2t6) def
  (04XXXX000B0000XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX03020) GRIBvariable
  :readdatafile
  /long_name (2-meter Minimum Temperature) def
  /standard_name (air_temperature) def
  /units (K) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

% /2dt { [ X Y last first subgrid | LA S ] (/Data/data25/ECMWF/S2S/ECMF/CY48/REL/CF/%Y[S]/%m[S]/ecmf_rel_cf_da_sfc%Y%m%d[S]%Y%m%d[S].grb) [ S ] readdatafile:
%   /name (DPT) def
%   (04XXXX000B0006) GRIBvariable
%   :readdatafile
%   /long_name (2-meter Dewpoint Temperature) def
%   /standard_name (dew_point_temperature) def
%   /units (K) def
%   /missing_value 9.999000E+20 def
%   decompress
%   Y last first RANGE
% } defasvarsilent

:dataset

% need to deal with land surface data

\end{ingrid}

\begin{ingrid}
dataset:

datasetdefs:
L name exch def
:datasetdefs

[ X Y ] {name exch def } forall

/name /land_sfc def

% /slt { [ X Y last first subgrid | L ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc2023121320231213.grb) readdatafile:
%   /name (SOTYP) def
%   (04XXXX00010300) GRIBvariable
%   :readdatafile
%   L first VALUE L removeGRID
%   /long_name (Soil Type) def
%   /standard_name (soil_type) def
%   /units (ids) def
%   /missing_value 9.999000E+20 def
%   decompress
%   Y last first RANGE
% } defasvarsilent

/lsm { [ X Y last first subgrid | L ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc2023121320231213.grb) readdatafile:
  /name (lsm) def
  (04XXXX00010000) GRIBvariable
  :readdatafile
  L first VALUE L removeGRID
  /long_name (Land-Sea Mask) def
  /units (unitless) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

/orog { [ X Y last first subgrid | L ] (/Data/data25/ECMWF/S2S/CPTEC/REL/CF/cptec_rel_cf_sfc_sfc2023121320231213.grb) readdatafile:
  /name (orog) def
  (04XXXX00010305) GRIBvariable
  :readdatafile
  L first VALUE L removeGRID
  /long_name (Orography) def
  /standard_name (geopotential_height) def
  /units (gpm) def
  /missing_value 9.999000E+20 def
  decompress
  Y last first RANGE
} defasvarsilent

:dataset

\end{ingrid}

:dataset


\end{ingrid}


