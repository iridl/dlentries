\begin{ingrid}
continuedataset:
/description (operational ECHAM4.5 ensemble forecasts based on persisted SST) def
datasetdefs:
\end{ingrid}
This is the old def -- now we have an explicit directory so that we can
control the permissions differently for different months.

Activated 12/02/2014 because MONTHLY dir is broken
\begin{ingrid}
/MONTHLY { SIX-HOURLY T 0.125 shiftGRID monthlyAverage
} defasvarcacheKeepRestrictions
\end{ingrid}
\begin{ingrid}
/DAILY { SIX-HOURLY T 0.125 shiftGRID T dailyAverage

 } defasvarKeepRestrictions
:datasetdefs
/SIX-HOURLY currentobject 1 index get def
/MONTHLY currentobject 1 index get def
/DAILY currentobject 1 index get def
\end{ingrid}
flushed cache for t2m, prcp from Dec 2010 onwards    2012-11-07
flushed cache for t2m, prcp for everything (cache files were too long, 
so each update was bringing in bad data)   2012-11-07
\begin{ingrid}
dataset:
/name /monthly def
/OperationalForecasts datadownloadsareIRIonlyPassword
%(ecm4/psst) IRIforecastdir deflastmod
/Y 64 namedgaussianlat
 high low subgrid 
name exch def
grid: /name /X def degE periodic 0 2.8125 357.1875 :grid
grid:
/name /M def
/long_name (member) def
/units (ids) def
1 1 24
:grid
grid:
/name /L def
/units (months) def
0.5 1 3.5
:grid
grid:
/name /S def
monthtime
/long_name (Forecast Started) def
/pointwidth 0 def
1 Sep 2001 ensotime
1.
1
IRImaxstarttime interp
ensotime
:grid
grid:
/name /P def
/units (mb) def
values: 200 500 700 850 :values
:grid
setrunoffset
S
 (260) (ecm4/psst) IRIlastforecastfilenameREU
removeextrausing
IRIforecastexpires
name exch def
/last_modified S .last_modified def
/expires S .expires def
/T { L S add [L S] REORDER /long_name (Forecast Time) def
/pointwidth 1 def } defasvar
/t2m { (167) (ecm4/psst) 
%IRIforecastfilename [ X Y ] readr4fromascii
%/last_modified 3 index .last_modified def
IRIforecastfilenameCS readdatafile:
r*4asascii :readdatafile
/long_name (Temperature at 2m)def
/units (Kelvin_scale) preferredunitname def
} defasvarcachesilent
/prcp { (260) (ecm4/psst) 
%IRIforecastfilename [ X Y ] readr4fromascii
%/last_modified 3 index .last_modified def
IRIforecastfilenameCS readdatafile:
r*4asascii :readdatafile
/long_name (precipitation rate) def
/units (m/s) preferredunitname def
} defasvarcachesilent
/zg { (156) (ecm4/psst) IRIforecastfilenameCS_P readdatafile:
/long_name (geopotential height) def
/units (m) preferredunitname def
/last_modified 3 index .last_modified def
r*4asascii :readdatafile
}defasvarcachesilent
:dataset
\end{ingrid}
\begin{ingrid}
:dataset
\end{ingrid}
