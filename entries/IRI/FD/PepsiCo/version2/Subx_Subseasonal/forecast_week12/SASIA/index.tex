\begin{ingrid}

continuedataset:

	/target_date { S L add /long_name (forecast time) def /standard_name (time) def } defasvar

:dataset

 MME_Precipitation_ELR .S name exch def
 MME_Precipitation_ELR .L name exch def

\end{ingrid}
