\begin{ingrid}

continuedataset:

/description (ELR forecast coefficients) def

grid:
/name /ind def
/long_name (Coefficients Index) def
0 1 2
:grid

/target_date 1 index .dataset  .dataset  .target_date defasvar


dataset:
/name /Precipitation def

    [ S L ind ] { name exch def } forall

    grid:
        /name /X def
        /long_name (Longitude) def
        /units /degree_east def
        85.5 1.0 114.5
    :grid

    grid:
        /name /Y def
        /long_name (Latitude) def
        /units /degree_north def
        2.5 1.0 27.5
    :grid

    /precip {
    [ X Y last first subgrid ind L | S ]
    (/Data/data21/iri/FD/PepsiCo/Subx_forecast/flexible_forecast/weekly_precip_MME/Subx_THAI_precip_issue_Fri_%d_%b_%Y[S]_weekly_fcst_coeff_MMEmean.nc) [S]
    readdatafile:
    /name (prob) def
    /doublearraytype netcdfrecords
    :readdatafile
    Y last first RANGE
    /missing_value NaN def
    /long_name (Precipitation Forecast Coefficients) def
    }defasvarsilent
:dataset

dataset:
/name /T2M def

    [ S L ind ] { name exch def } forall

    grid:
        /name /X def
        /long_name (Longitude) def
        /units /degree_east def
        85.0 1.0 115.0
    :grid

    grid:
        /name /Y def
        /long_name (Latitude) def
        /units /degree_north def
        2.0 1.0 28.0
    :grid

    /temperature {
    [ X Y last first subgrid ind L | S ]
    (/Data/data21/iri/FD/PepsiCo/Subx_forecast/flexible_forecast/weekly_T2M_MME/Subx_THAI_t2m_issue_Fri_%d_%b_%Y[S]_weekly_fcst_coeff_MMEmean.nc) [S]
    readdatafile:
    /name (prob) def
    /doublearraytype netcdfrecords
    :readdatafile
    Y last first RANGE
    /missing_value NaN def
    /long_name (Temperature Forecast Coefficients) def
    }defasvarsilent
:dataset

:dataset

\end{ingrid}
