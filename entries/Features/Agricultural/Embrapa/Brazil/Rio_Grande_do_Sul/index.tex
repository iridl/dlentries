\begin{ingrid}
continuedataset:

dataset:

/name /solos_tipo def
/description (Tipo Solos Rio Grande do Sul) def
/codigo
{IRIDB
   (solos_tipo_rs) (codigo) [ (gid) ]open_column_by
            /long_name (Codigo) def
}defasvarsilentnoreuse
/sbcs
{IRIDB
   (solos_tipo_rs) (sbcs) [ (gid) ]open_column_by
            /long_name (sbcs) def
}defasvarsilentnoreuse
/solo
{IRIDB
   (solos_tipo_rs) (solo) [ (gid) ]open_column_by
            /long_name (Solo) def
}defasvarsilentnoreuse
/solos
{IRIDB
   (solos_tipo_rs) (solos) [ (gid) ]open_column_by
            /long_name (Solos) def
}defasvarsilentnoreuse
/textura
{IRIDB
   (solos_tipo_rs) (textura) [ (gid) ]open_column_by
            /long_name (Textura) def
}defasvarsilentnoreuse
/tipo
{IRIDB
   (solos_tipo_rs) (tipo) [ (gid) ]open_column_by
            /long_name (Tipo) def
}defasvarsilentnoreuse
/the_geom
{IRIDB
   (solos_tipo_rs) (the_geom) [ (gid) ]open_column_by
            /long_name (Geometry) def
}defasvarsilentnoreuse

:dataset

\end{ingrid}
\begin{ingrid}

dataset:

/name /join def
/description (Join Rio Grande do Sul) def
/aptidão
{IRIDB
   (join_rs) (aptidão) [ (gid) ]open_column_by
            /long_name (Aptidão) def
}defasvarsilentnoreuse
/autonumber
{IRIDB
   (join_rs) (autonumber) [ (gid) ]open_column_by
            /long_name (Autonumber) def
}defasvarsilentnoreuse
/edafica
{IRIDB
   (join_rs) (edafica) [ (gid) ]open_column_by
            /long_name (Edafica) def
}defasvarsilentnoreuse
/i
{IRIDB
   (join_rs) (i) [ (gid) ]open_column_by
            /long_name (i) def
}defasvarsilentnoreuse
/id1
{IRIDB
   (join_rs) (id1) [ (gid) ]open_column_by
            /long_name (ID 1) def
}defasvarsilentnoreuse
/legenda
{IRIDB
   (join_rs) (legenda) [ (gid) ]open_column_by
            /long_name (Legenda) def
}defasvarsilentnoreuse
/legenda_1
{IRIDB
   (join_rs) (legenda_1) [ (gid) ]open_column_by
            /long_name (Legenda 1) def
}defasvarsilentnoreuse
/m
{IRIDB
   (join_rs) (m) [ (gid) ]open_column_by
            /long_name (m) def
}defasvarsilentnoreuse
/n18
{IRIDB
   (join_rs) (n18) [ (gid) ]open_column_by
            /long_name (N 18) def
}defasvarsilentnoreuse
/n19
{IRIDB
   (join_rs) (n19) [ (gid) ]open_column_by
            /long_name (N 19) def
}defasvarsilentnoreuse
/p
{IRIDB
   (join_rs) (p) [ (gid) ]open_column_by
            /long_name (p) def
}defasvarsilentnoreuse
/pct
{IRIDB
   (join_rs) (pct) [ (gid) ]open_column_by
            /long_name (pct) def
}defasvarsilentnoreuse
/r
{IRIDB
   (join_rs) (r) [ (gid) ]open_column_by
            /long_name (r) def
}defasvarsilentnoreuse
/codigo
{IRIDB
   (join_rs) (codigo) [ (gid) ]open_column_by
            /long_name (Codigo) def
}defasvarsilentnoreuse
/sbcs
{IRIDB
   (join_rs) (sbcs) [ (gid) ]open_column_by
            /long_name (sbcs) def
}defasvarsilentnoreuse
/solo
{IRIDB
   (join_rs) (solo) [ (gid) ]open_column_by
            /long_name (Solo) def
}defasvarsilentnoreuse
/solos
{IRIDB
   (join_rs) (solos) [ (gid) ]open_column_by
            /long_name (Solos) def
}defasvarsilentnoreuse
/textura
{IRIDB
   (join_rs) (textura) [ (gid) ]open_column_by
            /long_name (Textura) def
}defasvarsilentnoreuse
/tipo
{IRIDB
   (join_rs) (tipo) [ (gid) ]open_column_by
            /long_name (Tipo) def
}defasvarsilentnoreuse
/the_geom
{IRIDB
   (join_rs) (the_geom) [ (gid) ]open_column_by
            /long_name (Geometry) def
}defasvarsilentnoreuse

:dataset

\end{ingrid}
\begin{ingrid}

dataset:

/name /area_tipo def
/description (Area Tipo Rio Grande do Sul) def

/area_ha
{IRIDB
   (area_tipo_rs) (area_ha) [ (gid) ]open_column_by
            /long_name (Area) def
	    /units (hectare) def
}defasvarsilentnoreuse
/Área
{IRIDB
   (area_tipo_rs) (Área) [ (gid) ]open_column_by
            /long_name (Área) def
}defasvarsilentnoreuse
/tipo
{IRIDB
   (area_tipo_rs) (tipo) [ (gid) ]open_column_by
            /long_name (Tipo) def
}defasvarsilentnoreuse
/the_geom
{IRIDB
   (area_tipo_rs) (the_geom) [ (gid) ]open_column_by
            /long_name (Geometry) def
}defasvarsilentnoreuse

:dataset

\end{ingrid}
\begin{ingrid}

dataset:

/name /solos def
/description (Solos Rio Grande do Sul) def

/comp_1
{IRIDB
   (solos_dd) (comp_1) [ (gid) ]open_column_by
            /long_name (COMP 1) def
}defasvarsilentnoreuse
/comp_2
{IRIDB
   (solos_dd) (comp_2) [ (gid) ]open_column_by
            /long_name (COMP 2) def
}defasvarsilentnoreuse
/comp_3
{IRIDB
   (solos_dd) (comp_3) [ (gid) ]open_column_by
            /long_name (COMP 3) def
}defasvarsilentnoreuse
/fid_1
{IRIDB
   (solos_dd) (fid_1) [ (gid) ]open_column_by
            /long_name (FID 1) def
}defasvarsilentnoreuse
/n_2_sibcs
{IRIDB
   (solos_dd) (n_2_sibcs) [ (gid) ]open_column_by
            /long_name (nome 2 SIBCS) def
}defasvarsilentnoreuse
/n_3_sibc_1
{IRIDB
   (solos_dd) (n_3_sibc_1) [ (gid) ]open_column_by
            /long_name (nome 3 SIBC 1) def
}defasvarsilentnoreuse
/n_3_sibcs
{IRIDB
   (solos_dd) (n_3_sibcs) [ (gid) ]open_column_by
            /long_name (nome 3 SIBCS) def
}defasvarsilentnoreuse
/nivel1
{IRIDB
   (solos_dd) (nivel1) [ (gid) ]open_column_by
            /long_name (Nivel 1) def
}defasvarsilentnoreuse
/nova_leg
{IRIDB
   (solos_dd) (nova_leg) [ (gid) ]open_column_by
            /long_name (Nova leg) def
}defasvarsilentnoreuse
/sibcs_desc
{IRIDB
   (solos_dd) (sibcs_desc) [ (gid) ]open_column_by
            /long_name (SIBCS Description) def
}defasvarsilentnoreuse
/simbolo
{IRIDB
   (solos_dd) (simbolo) [ (gid) ]open_column_by
            /long_name (Simbolo) def
}defasvarsilentnoreuse
/textura
{IRIDB
   (solos_dd) (textura) [ (gid) ]open_column_by
            /long_name (Textura) def
}defasvarsilentnoreuse
/the_geom
{IRIDB
   (solos_dd) (the_geom) [ (gid) ]open_column_by
            /long_name (Geometry) def
}defasvarsilentnoreuse

:dataset

\end{ingrid}
\begin{ingrid}

:dataset

solos_tipo sbcs .gid name exch put
join sbcs .gid name exch put
area_tipo area_ha .gid name exch put
solos fid_1 .gid name exch put

\end{ingrid}

