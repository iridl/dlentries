\begin{ingrid}

/description (Rivers of the Philippines) def

continuedataset:

/f_code {
IRIDB (phl_hyd_rivers_vmap0) (f_code) [(objectid)] open_column_by
/long_name (F Code) def
}defasvarsilentnoreuse

/fac_id {
IRIDB (phl_hyd_rivers_vmap0) (fac_id) [(objectid)] open_column_by
/long_name (fac ID) def
}defasvarsilentnoreuse

/globalid {
IRIDB (phl_hyd_rivers_vmap0) (globalid) [(objectid)] open_column_by
/long_name (Global ID) def
}defasvarsilentnoreuse

/hyc {
IRIDB (phl_hyd_rivers_vmap0) (hyc) [(objectid)] open_column_by
}defasvarsilentnoreuse

/nam {
IRIDB (phl_hyd_rivers_vmap0) (nam) [(objectid)] open_column_by
/long_name (Name) def
}defasvarsilentnoreuse

/leng {
IRIDB (phl_hyd_rivers_vmap0) (shape_leng) [(objectid)] open_column_by
/long_name (Length) def
/units (km) def
}defasvarsilentnoreuse

/tile_id {
IRIDB (phl_hyd_rivers_vmap0) (tile_id) [(objectid)] open_column_by
/long_name (Tile ID) def
}defasvarsilentnoreuse

/the_geom {
IRIDB (phl_hyd_rivers_vmap0) (geom) [(objectid)] open_column_by
/long_name (Geometry) def
}defasvarsilentnoreuse

/label {
IRIDB (phl_hyd_rivers_vmap0) (nam) [(objectid)] open_column_by
/long_name (Label) def
}defasvarsilentnoreuse

:dataset

fac_id .objectid name exch def

\end{ingrid}
