\begin{ingrid}
continuedataset:
/name /districts_pakistan def
/first_dist
{IRIDB
   (districtpk98) (first_dist) [ (first_code) ]open_column_by
            /long_name (District Name) def
}defasvarsilentnoreuse
/first_pro
{IRIDB
   (districtpk98) (first_pro) [ (first_code) ]open_column_by
            /long_name (Province ID) def
}defasvarsilentnoreuse
/first_prov
{IRIDB
   (districtpk98) (first_prov) [ (first_code) ]open_column_by
            /long_name (Province Name) def
}defasvarsilentnoreuse
/the_geom
{IRIDB
   (districtpk98) (the_geom) [ (first_code) ]open_column_by
            /long_name (District geometry) def
}defasvarsilentnoreuse
:dataset
\end{ingrid}
