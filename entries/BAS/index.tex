\begin{ingrid}

continuedataset:

datasetdefs:
grid:
/name /micol def
1 1 1
:grid
:datasetdefs

%DEFINE MONTHLY DATA
    grid:
      /name /T def
      monthtime
      /defaultvalue { last } def
      /pointwidth 1 def
       /standard_name (time) def
      /long_name (SAM | Real-Time) def
      values:
        16 Jan 1957 ensotime
        1
        systemtime
        systemtime2ymd
        ymd:lastmonth
        ymd:nextmonth 15 add
        ymd2rm1960
        {} for
      :values

    :grid

T { (/Data/data1/noaa/indices/sam/sam-had_long/sam_%Y[T]-%m[T].txt) [T]  } removeextrausing
/expires
  T last cvsunits
    interp exch
     0.5 add
     6 ymd:nextmonth
      5 add
      ymd2systemtime
      nip
def
  name exch def
/expires T .expires  def


link:
  /name (website) def
  /description (https://legacy.bas.ac.uk/met/gjma/sam.html)def
  /href (https://legacy.bas.ac.uk/met/gjma/sam.html)def
:link


/institution (British Antarctic Survey | BAS) def
/history (Monthly mean surface temperature data and derived statistics for some Antarctic stations) def
/description (British Antarctic Survey ) def 


/SAM {
 [ micol | T  ]
  (/Data/data1/noaa/indices/sam/sam-had_long/sam_%Y[T]-%m[T].txt) [T] 
 readdatafile:
  [ 3 1 ]r*8astsv
 :readdatafile
   micol 1. VALUE
   /units /degree_Celsius def
   /missing_value -999.00 def 
   /long_name (Southern Annular Mode) def
   temp_anomaly_colors
   DATA AUTO AUTO RANGE
   micol removeGRID

 /title (Southern Annular Mode) def
 /description (An observation-based Southern Hemisphere Annular Mode Index) def

 link:
  /name (ftp_url) def
  /description (http://www.nerc-bas.ac.uk/public/icd/gjma/newsam.1957.2007.txt) def
  /href (http://www.nerc-bas.ac.uk/public/icd/gjma/newsam.1957.2007.txt) def
 :link

}defasvarsilent

:dataset
\end{ingrid}

