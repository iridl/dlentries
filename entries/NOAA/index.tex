\begin{ingrid}
datasetdefs:
/DAILY { (http://ingrid.ldgo.columbia.edu/SOURCES/.NOAA/.NCDC/.DAILY/)
  redirectto } def
:datasetdefs
dup /ESRL get /ESRL exch def
dup /NCDC get /NCDC exch def
dup /NGDC get /NGDC exch def
dup /NODC get /NODC exch def
dup /NCEP-NCAR get /NCEP-NCAR exch def
dup /NCEP-DOE get /NCEP-DOE exch def
dup /NCEP get /NCEP exch def
dup /NESDIS get /NESDIS exch def
dup /PSL get /PSL exch def
dup /NCDC  get /NCEI  exch def
/description (National Oceanic and Atmospheric Administration) def
/iridl:hasSemantics (iridl:NOAA) def
\end{ingrid}
