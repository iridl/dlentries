\begin{ingrid}
/INMETAgroClima datadownloadsareRestricted
continuedataset:

/description (Solo geo do Brazil) def

/slgclasse {
  IRIDB
  (sologeo_dd) (slgclasse) [ (gid) ]open_column_by
  /long_name (classe solo geo) def
}defasvarsilentnoreuse

/slgcodigo {
  IRIDB
  (sologeo_dd) (slgcodigo) [ (gid) ]open_column_by
  /long_name (codigo solo geo) def
}defasvarsilentnoreuse

/slgdescric {
  IRIDB
  (sologeo_dd) (slgdescric) [ (gid) ]open_column_by
  /long_name (descrição solo geo) def
}defasvarsilentnoreuse

/slgsubclas {
  IRIDB
  (sologeo_dd) (slgsubclas) [ (gid) ]open_column_by
  /long_name (sub classe solo geo) def
}defasvarsilentnoreuse

/the_geom {
  IRIDB
  (sologeo_dd) (the_geom) [ (gid) ]open_column_by
  /long_name (geometry) def
}defasvarsilentnoreuse

/the_google {
  IRIDB
  (sologeo_dd) (the_google) [ (gid) ]open_column_by
  /long_name (the google) def
}defasvarsilentnoreuse

:dataset
\end{ingrid}

