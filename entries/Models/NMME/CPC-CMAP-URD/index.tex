\begin{ingrid}
continuedataset:

link:
/name (source) def
/href (ftp://ftp.cpc.ncep.noaa.gov/NMME/obs/) def
:link

grid:
/name /X def
/long_name (longitude) def
/standard_name (longitude) def
periodic
0.0 1.0 359.0
/units (degree_east) def
:grid

grid:
/name /Y def
/long_name (latitude) def
/standard_name (latitude) def
-90.0 1.0 90.0
/units (degree_north) def
:grid

grid:
/name /T def
/long_name (time) def
/standard_name (time) def
/units (months since 1960-01-01) def
16 Jan 1982 ensotime
1.0
16 Dec 2014 ensotime
:grid

/prate { [ X Y | T ] (/Data/data23/NMME/Obs.D/precip.1x1.dat) readdatafile:
/name (prate) def
/units (mm/month) def
/long_name (Total Precipitation (CMAP(ocean) + CPC-URD(land)) ) def
/standard_name (lwe_precipitation_rate) def
/missing_value -999.0 def
decdirect :readdatafile
/description (Monthly CMAP precipitation over the ocean and CPC Uniified Raingauge Database over land) def
} defasvarsilent 

:dataset
\end{ingrid}
